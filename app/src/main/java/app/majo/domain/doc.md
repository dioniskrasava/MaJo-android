

# 🟧  **Domain слой**

➡ *Папка domain*
➡ *Это “мозг” приложения*

DOMAIN — это **ядро, смысл, логика приложения**.

Как правильно перевести?

## ✔ Лучший перевод:

# **Domain = Предметная область**

или

# **Domain = Смысловая область (логика приложения)**

Это НЕ "домен" в смысле сайт.ru
И НЕ доменное имя.

Domain = *что приложение делает, а не как оно устроено.*

### Что находится в Domain?

1. **Модели (Action, ActionRecord, UnitType, ActionCategory…)**
   Это чистые структуры данных.

2. **Интерфейсы репозиториев**
   Например:

```kotlin
interface ActionRepository {
    fun getActions(): Flow<List<Action>>
    suspend fun insert(action: Action)
}
```

3. **Бизнес-логика**
   Например:

```kotlin
object PointsCalculator { ... }
```

Этот слой не знает:

* ни про UI,
* ни про Room,
* ни про сеть.

Domain — полностью независимым.
Ты мог бы запустить Domain в консоли без Android.

---

---

# 📌 Если совсем коротко:

### **UI** — показывает

### **Domain** — думает

### **Data** — хранит



# 📘 Подведём итог про слово Domain

## Domain = *Предметная область, смысловая часть приложения*.

Это правила, логика, модели и интерфейсы.

Это самый правильный перевод, который используют в профессиональной среде программистов.

