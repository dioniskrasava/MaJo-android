
# 🟩  **Data слой**

➡ *Папка data*
➡ *Реализация доступа к данным*

Это “как” мы получаем данные:

* Room (база)
* Retrofit (интернет)
* Fake репозитории
* SharedPreferences / DataStore
* локальные файлы

### Data отвечает за хранение и получение данных.

Пример:

* есть интерфейс **ActionRepository** в domain
* есть его реализация **ActionRepositoryImpl** в data

В data хранятся:

* **DTO / Entity (структуры базы)**
* **DAO (Room)**
* **мапперы**

Чтобы превратить:

```
ActionEntity (база)
     ⇅
Action (домен)
```

---

# 🔵 Важно: зависимости идут только в одну сторону:

```
UI  →  Domain  →  Data
```

никогда наоборот.

## ❌ Domain не должен знать про Data

(домен не знает про Room, Retrofit, Android)

## ❌ UI не должен знать про Data напрямую

(UI должен обращаться только через ViewModel → Domain)
